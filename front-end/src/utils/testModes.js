const front_first = "front-first";
const back_first = "back-first";
const random = "random";

const testModes = {
  FRONT_FIRST: front_first,
  BACK_FIRST: back_first,
  RANDOM: random
};

export default testModes;
